package utils;

import org.json.JSONException;
import org.json.JSONObject;

public class DataProcessHelper {
	public static void process(JSONObject json) throws JSONException
	{
		/*
		Student.deleteAll();
		Course.deleteAll();
		Batch.deleteAll();
		Curriculum.deleteAll();
		Section.deleteAll();
		Subject.deleteAll();
		StudentSection.deleteAll();
		Timeslot.deleteAll();
		Fee.deleteAll();
		Setting.deleteAll();
					
		StudentFromJsonConverter.convert(json.getJSONObject("student")).save();
		CourseFromJsonConverter.convert(json.getJSONObject("course")).save();
		BatchFromJsonConverter.convert(json.getJSONObject("batch")).save();
		CurriculumFromJsonConverter.convert(json.getJSONObject("curriculum")).save();
		for(StudentSection item:StudentSectionFromJsonConverter.convertList(json.getJSONArray("student_sections")))item.save();
		for(Section item:SectionFromJsonConverter.convertList(json.getJSONArray("sections")))item.save();
		for(Subject item:SubjectFromJsonConverter.convertList(json.getJSONArray("subjects")))item.save();
		for(Timeslot item:TimeslotFromJsonConverter.convertList(json.getJSONArray("timeslots"))){item.calcDuration();item.save();}
		for(Fee item:FeeFromJsonConverter.convertList(json.getJSONArray("fees")))item.save();
		for(Setting item:SettingFromJsonConverter.convertList(json.getJSONArray("settings")))item.save();
	*/
	}
}
